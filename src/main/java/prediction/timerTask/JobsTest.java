package prediction.timerTask;

import prediction.aop.BeanFactory;

public class JobsTest {
    public static void main(String[] args) {
        BeanFactory beanFactory = BeanFactory.INSTANCE;
        while (true) ;
    }
}